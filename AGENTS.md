Instead of defining constants u only use one, inline them.
Don't use if __name__ == 'main', do not wrap shit in main()
Avoid unnecessary type guards, abide by KISS principles.
If u define all caps constant imma unplug u
Use uv by astral
Running the whole proprocessing pipeline is really slow. Use random sampling when iterating.
use uv run jupyter nbconvert --execute --to notebook --inplace <a.ipynb> to run notebooks

This codebase will outlive you. Every shortcut becomes someone else's burden. Patterns you establish will be copied. Corners you cut will be cut again.

Fight entropy. Leave the codebase better than you found it. Fix issues you encounter, even if they are not directly related to your task.
